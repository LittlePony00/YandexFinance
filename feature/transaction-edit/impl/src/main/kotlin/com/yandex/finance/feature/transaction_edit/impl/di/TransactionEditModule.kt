package com.yandex.finance.feature.transaction_edit.impl.di

import com.yandex.finance.feature.transaction_edit.impl.presentation.viewmodel.TransactionEditViewModel
import dagger.Module

@Module
class TransactionEditModule {
    
    // No additional provides needed for now
    // TransactionEditViewModel uses @Inject constructor
}
